{
  "name": "chatapp",
  "private": true,
  "description": "Enterprise internal chat — npm workspaces monorepo (backend, desktop, admin)",
  "workspaces": [
    "backend",
    "desktop",
    "admin"
  ],
  "scripts": {
    "setup:env": "node scripts/setup-env.js",
    "setup": "npm run setup:env && npm install",
    "dev:backend": "npm run start:dev -w chatapp-backend",
    "dev:desktop": "npm run electron:dev -w chatapp-desktop",
    "dev:admin": "npm run dev -w chatapp-admin",
    "dev": "concurrently -n backend,desktop -c blue,magenta \"npm run dev:backend\" \"npm run dev:desktop\"",
    "dev:all": "concurrently -n backend,desktop,admin -c blue,magenta,cyan \"npm run dev:backend\" \"npm run dev:desktop\" \"npm run dev:admin\"",
    "dev:infra": "docker compose up -d postgres redis minio minio-init",
    "migrate": "npm run migrate -w chatapp-backend",
    "check:schema-drift": "npm run check:schema-drift -w chatapp-backend",
    "check:secrets": "npm run check:secrets -w chatapp-backend",
    "generate:secrets": "npm run generate:secrets -w chatapp-backend",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "build:backend": "npm run build -w chatapp-backend",
    "build:desktop": "npm run build -w chatapp-desktop",
    "build:admin": "npm run build -w chatapp-admin",
    "build": "npm run build -w chatapp-backend && npm run build -w chatapp-desktop && npm run build -w chatapp-admin",
    "lint:backend": "npm run lint -w chatapp-backend",
    "lint:desktop": "npm run lint -w chatapp-desktop",
    "lint:admin": "npm run lint -w chatapp-admin",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
